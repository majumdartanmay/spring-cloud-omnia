spring:
  cloud:
    omnia:
      server:
        prefix: myserver
        syncTime: 700000
        maxQueueSize: 2
        environment: TEST
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/omnia_logs?sessionVariables=sql_mode='NO_ENGINE_SUBSTITUTION'&jdbcCompliantTruncation=false
    username: root
    password: wart414ways465
  jpa:
    hibernate:
      ddl-auto: validate